'use strict';

window.addEventListener('load', () => {

    const controller = new Controller('name', 'email', 'subject', 'comment', 'name_info', 'email_info', 'subject_info', 'comment_info');
    controller.activateLiseners();





});
